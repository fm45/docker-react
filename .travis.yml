language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t fabienmoreau/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fabienmoreau/docker-react npm run test